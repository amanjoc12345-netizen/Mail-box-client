import React from 'react';
import { Card } from 'react-bootstrap';

const Products = () => (
  <Card className="signup-card text-center fade-in">
    <Card.Body>
      <h2 className="fw-bold mb-3" style={{ color: '#1e293b' }}>Products</h2>
      <p className="text-muted">Explore our digital web products and tools here.</p>
    </Card.Body>
  </Card>
);

export default Products;
